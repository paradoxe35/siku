<section id="hero" style="height: 430px">
    <div class="inner">
        <div class="{{ $container ?? 'container-fluid' }}">
            <h1 class="text-center {{ $display ?? 'display-2' }}">{{ __($title) }}</h1>
        </div>
    </div>
</section>